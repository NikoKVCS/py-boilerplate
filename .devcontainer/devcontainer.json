{
  "name": "Python Dev",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.black-formatter"
      ]
    }
  },
  "overrideCommand": false,
  "remoteUser": "root",
  "runArgs": ["--user", "root", "-e", "ENV=dev", "-p", "9875:9875"],
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "cd /workspace && poetry config virtualenvs.in-project true && poetry install"
}